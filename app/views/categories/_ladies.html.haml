.categoryindex__pc__box__white__name
  %h3#root_category-1
    = link_to "レディース"
.categoryindex__pc__box__white__inner
  %p
    = link_to ProductCategory.find(3).name, 'url'
  .categoryindex__pc__box__white__inner__name
    %h4
      トップス
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(2).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      ジャケット/アウター
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(22).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      パンツ
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(45).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      スカート
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(59).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      ワンピース
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(66).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      靴
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(72).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      ルームウェア/パジャマ
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(84).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      レッグウェア
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(88).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      帽子
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(95).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      バッグ
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(104).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      アクセサリー
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(124).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      ヘアアクセサリー
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(137).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      小物
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(143).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      時計
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(168).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      ウィッグ/エクステ
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(176).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      浴衣/水着
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(184).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      スーツ/フォーマル/ドレス
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(194).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      マタニティ
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(203).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
  .categoryindex__pc__box__white__inner__name
    %h4
      その他
  .clearfix.categoryindex__pc__box__white__inner__box
    - ProductCategory.find(214).children.each do |child|
      .categoryindex__pc__box__white__inner__box__sub
        %p
          = link_to child.name,category_path(child.id)
